PORT=4000
CORS_ORIGIN=https://azas-project.vercel.app
JWT_SECRET=change-me

# Auth DB (users, lot_defect_reports) - project DB와 동일 DB 사용 시 AUTH_DB_NAME=project
AUTH_DB_HOST=localhost
AUTH_DB_PORT=3306
AUTH_DB_USER=root
AUTH_DB_PASSWORD=
AUTH_DB_NAME=manufacturing_db

# 공정/대시보드 DB (preprocessing 테이블에서 조회)
# PROCESS_DB_NAME=project  또는  DB_NAME=project
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=factory
# PROCESS_DB_NAME=project
# 대시보드 공정 데이터 테이블 (기본값: preprocessing)
PROCESS_TABLE_NAME=preprocessing

# '오늘' 날짜 기준 타임존 (Lightsail UTC일 때 DB가 KST면 Asia/Seoul 권장)
BACKEND_DATE_TZ=Asia/Seoul

# Optional: Gemini (LOT 불량 리포트)
GOOGLE_GENERATIVE_AI_API_KEY=
GEMINI_MODEL_NAME=gemini-1.5-flash

