# 일일 평균 토큰 사용량 분석 (챗봇 미사용 시)

## 📊 토큰 사용 소스

### 1. 챗봇 질문 (사용자가 질문하지 않으면: **0 토큰**)
- 사용자가 챗봇에 질문하지 않으면 토큰 사용 없음
- **일일 사용량: 0 토큰**

### 2. 불량 LOT 자동 레포트 생성 (자동 실행)

#### 실행 조건
- Lot 상태 페이지 (`/lot-status`) 접속 시
- 불량 LOT 감지 시 자동으로 레포트 생성

#### 토큰 사용량 (레포트 1개당)
- **모델**: `gemini-pro` (또는 `gemini-1.5-flash`)
- **입력 토큰**: 약 150-250 토큰
  - LOT 데이터 (ID, 판정, 리튬 투입량, 첨가제 비율, 공정 시간, 습도, 탱크 압력 등)
  - 프롬프트 템플릿
- **출력 토큰**: 약 200-400 토큰
  - 불량 원인 분석 레포트 (요약, 원인 분석, 권장 조치)
- **총 토큰/레포트**: 약 **350-650 토큰**

#### 발생 빈도 예상
- **Lot 상태 페이지 접속 횟수**: 사용자마다 다름
  - 보수적 추정: 하루 10-20회 접속
  - 일반적 추정: 하루 30-50회 접속
  - 활발한 사용: 하루 100회 이상 접속

- **불량 LOT 발생률**: 제조 공정에 따라 다름
  - 일반적: 전체 LOT의 1-5% 불량
  - 보수적 추정: 하루 10-50개 불량 LOT
  - 일반적 추정: 하루 50-200개 불량 LOT
  - 높은 불량률: 하루 200-500개 불량 LOT

- **중복 레포트 방지**: DB에 저장되어 있으면 재생성하지 않음
  - 첫 접속 시에만 생성
  - 이후 접속 시에는 DB에서 조회만 함 (토큰 사용 없음)

## 📈 일일 평균 토큰 사용량 시나리오

### 시나리오 1: 보수적 추정 (낮은 사용량)
- Lot 상태 페이지 접속: 하루 10회
- 불량 LOT 발생: 하루 10개
- 새로 생성되는 레포트: 하루 5개 (50%는 이미 생성됨)
- **일일 토큰 사용량**: 5개 × 500 토큰 = **약 2,500 토큰**

### 시나리오 2: 일반적 추정 (보통 사용량)
- Lot 상태 페이지 접속: 하루 30회
- 불량 LOT 발생: 하루 50개
- 새로 생성되는 레포트: 하루 20개 (60%는 이미 생성됨)
- **일일 토큰 사용량**: 20개 × 500 토큰 = **약 10,000 토큰**

### 시나리오 3: 활발한 사용 (높은 사용량)
- Lot 상태 페이지 접속: 하루 100회
- 불량 LOT 발생: 하루 200개
- 새로 생성되는 레포트: 하루 50개 (75%는 이미 생성됨)
- **일일 토큰 사용량**: 50개 × 500 토큰 = **약 25,000 토큰**

### 시나리오 4: 최악의 경우 (매우 높은 사용량)
- Lot 상태 페이지 접속: 하루 200회
- 불량 LOT 발생: 하루 500개
- 새로 생성되는 레포트: 하루 100개 (80%는 이미 생성됨)
- **일일 토큰 사용량**: 100개 × 500 토큰 = **약 50,000 토큰**

## 💡 토큰 사용량 최적화 방안

### 현재 최적화 상태
✅ **캐싱**: DB에 저장된 레포트는 재생성하지 않음
✅ **선택적 생성**: 불량 LOT만 레포트 생성
✅ **챗봇 비활성화**: 사용자가 질문하지 않으면 토큰 사용 없음

### 추가 최적화 가능 사항
1. **레포트 생성 제한**
   - 하루에 생성할 수 있는 레포트 수 제한
   - 예: 하루 최대 20개 레포트만 생성

2. **배치 처리**
   - 여러 불량 LOT을 한 번에 처리하여 프롬프트 최적화
   - 예: 5개 LOT을 한 번에 분석

3. **레포트 생성 조건 강화**
   - 중요도가 높은 불량 LOT만 레포트 생성
   - 예: 특정 불량 유형만 선택

4. **사용자 수동 생성**
   - 자동 생성 대신 사용자가 필요할 때만 생성 버튼 클릭

## 📊 예상 일일 토큰 사용량 요약

| 시나리오 | 불량 LOT 발생 | 새 레포트 생성 | 일일 토큰 사용량 |
|---------|-------------|--------------|----------------|
| 보수적 | 10개 | 5개 | **약 2,500 토큰** |
| 일반적 | 50개 | 20개 | **약 10,000 토큰** |
| 활발한 | 200개 | 50개 | **약 25,000 토큰** |
| 최악의 경우 | 500개 | 100개 | **약 50,000 토큰** |

## 🎯 결론

**챗봇을 사용하지 않을 때 일일 평균 토큰 사용량:**
- **최소**: 약 **2,500 토큰** (보수적 추정)
- **일반적**: 약 **10,000 토큰** (일반적 추정)
- **최대**: 약 **50,000 토큰** (최악의 경우)

**참고:**
- Google Gemini API 무료 티어 제한은 프로젝트마다 다르지만, 일반적으로 일일 15-60 requests/min 제한이 있습니다.
- 토큰 제한은 모델과 플랜에 따라 다릅니다.
- 현재 설정으로는 **일반적으로 하루 10,000 토큰 정도** 사용할 것으로 예상됩니다.
